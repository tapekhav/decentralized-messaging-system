syntax = "proto3";

message ClientMessage {
    string nickname = 1;
    string message = 2;
}

message ServerResponse {
    string opponent_ip = 1;
    string session_id = 2;
}

service ServerService {
    rpc SendMessage(ClientMessage) returns (ClientMessage);
}