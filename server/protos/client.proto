syntax = "proto3";

package server;

service SessionService {
    rpc getSession(ClientMessage) returns (ServerResponse) {}
}

message ClientMessage {
    string nickname = 1;
}

message Session {
    uint32 session_id = 1;
    string opponent_ip = 2;
}

message ServerResponse {
    Session session = 1;
}