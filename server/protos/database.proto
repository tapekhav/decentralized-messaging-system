syntax = "proto3";

package server;

service IpDatabaseService {
    rpc getIp(IpRequest) returns (IpResponse) {};
}

message IpRequest {
    string ip = 1;
} 

message IpResponse {
    string result = 1;
}